<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.monkey.springboottemplate.modules.user.dao.UserDAO">

    <select id="queryUserByIdList" parameterType="com.monkey.springboottemplate.modules.user.vo.UserPageInputVO" resultType="com.monkey.springboottemplate.modules.user.vo.BaseUserVO">
        SELECT
        id,
        user_id,
        username,
        sex
        FROM
        tb_user
        WHERE
        1 = 1
        <if test="idList != null and idList.size()>0">
            AND id in
            <foreach item="item" index="index" collection="idList" open="(" separator="," close=")">
                #{item}
            </foreach>
        </if>
    </select>

</mapper>